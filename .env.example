# prisma/schema.prisma の url / directUrl
# Supabase: ダッシュボード Connect の URI をそのまま使う（記号は URI エンコード）
DATABASE_URL="postgresql://..."
DIRECT_URL="postgresql://..."

AUTH_SECRET="openssl rand -base64 32 等で生成"

# 接続確認: npm run db:check
